name: 🛒 DemoBlaze 1% Elite CI Pipeline

on:
  push:
    branches: [ "main" ]
    paths:
      - 'ecommerce-demoblaze/demoblaze-tests/src/**'
      - 'ecommerce-demoblaze/demoblaze-tests/pom.xml'
      - 'docker-compose.yml'
      - '.github/workflows/main.yml' 
  workflow_dispatch: 

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout Code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: ☕ Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven

    - name: 🏗️ Start Docker Selenium Grid
      run: |
        docker compose up -d --wait --wait-timeout 120
        sleep 15
        curl -f http://localhost:4444/wd/hub/status || echo "Grid warmup..."

    - name: 🧪 Run Maven Tests
      env:
        SPLUNK_TOKEN: ${{ secrets.SPLUNK_TOKEN }}
      working-directory: ecommerce-demoblaze/demoblaze-tests
      run: |
        echo "::add-mask::$SPLUNK_TOKEN"
        # Pre-create directory to avoid permission issues
        mkdir -p target/reports logs
        # -Duser.dir ensures System.getProperty("user.dir") anchors to the submodule
        mvn clean test-compile test -Dheadless=true \
          -Dexecution_env=remote \
          -Dsplunk.token=$SPLUNK_TOKEN \
          -Dreport.dir=target/reports \
          -Duser.dir=$(pwd) \
          -Dmaven.test.failure.ignore=true

    - name: 📊 Upload Test Artifacts
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: execution-reports
        # Captures BOTH Surefire and Extent reports for comparison
        path: |
          ecommerce-demoblaze/demoblaze-tests/target/surefire-reports/
          ecommerce-demoblaze/demoblaze-tests/target/reports/
        retention-days: 7

    - name: 🧪🔍 FINAL DEBUG - Locate Reports
      if: always()
      working-directory: ecommerce-demoblaze/demoblaze-tests
      run: |
        echo "=== CHECKING FOR ANY HTML REPORT ==="
        # Search everywhere in target in case it's in a nested folder
        find target/ -name "*.html"
        
        echo "=== SUREFIRE VERIFICATION ==="
        ls -la target/surefire-reports/index.html || echo "❌ Surefire index missing"
        
        echo "=== EXTENT VERIFICATION ==="
        ls -la target/reports/ || echo "❌ Extent folder empty"
